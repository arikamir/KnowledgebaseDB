apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Rendered as the second source of the non-production Argo CD Application.
# Keeping this source separate preserves the explicit career-migrations
# namespace while the service source remains pinned to career-agent.
resources:
  - core-migration-presync.yaml
