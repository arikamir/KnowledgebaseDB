apiVersion: v1
kind: Service
metadata:
  name: devops-career-agent
  labels:
    app.kubernetes.io/name: devops-career-agent
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: devops-career-agent
  ports:
    - name: http
      port: 80
      targetPort: http
