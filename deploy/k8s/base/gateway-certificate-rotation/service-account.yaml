apiVersion: v1
kind: ServiceAccount
metadata:
  name: gateway-certificate-rotation
  namespace: career-agent
  annotations:
    azure.workload.identity/client-id: ${GATEWAY_CERTIFICATE_DNS_CLIENT_ID}
  labels:
    azure.workload.identity/use: "true"
automountServiceAccountToken: true
