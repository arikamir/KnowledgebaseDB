apiVersion: v1
kind: ServiceAccount
metadata:
  name: evidence-hold-reconciler
  namespace: evidence-hold-system
  annotations:
    azure.workload.identity/client-id: ${EVIDENCE_HOLD_RECONCILER_CLIENT_ID}
  labels:
    azure.workload.identity/use: "true"
automountServiceAccountToken: false
