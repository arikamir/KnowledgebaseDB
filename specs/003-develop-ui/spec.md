# Feature Specification: DevOps Career Agent UI

**Feature Branch**: `003-develop-ui`  
**Created**: 2026-07-11  
**Status**: Approved for implementation  
**Input**: User description: "Create a feature for developing a UI; the UI, Backend-for-Frontend, and core backend must be separately hosted services"

## Clarifications

### Session 2026-07-11

- Q: Which login and identity mechanism must the UI use? -> A: Microsoft Entra ID organizational sign-in.
- Q: What duration should each focused learning session target? -> A: 20-30 minutes.
- Q: How should the first release provide hands-on lab experience? -> A: Curated links to external hands-on labs.
- Q: How should review questions determine learning-session completion? -> A: Use 3-5 scored questions, an 80% threshold, immediate feedback, and retries.
- Q: Which motivation model should encourage continued learning? -> A: Use visible milestones, completion celebrations, and one recommended next action.
- Q: How must the UI be hosted relative to the backend service? -> A: The UI must be deployed and operated as a separate application.
- Q: Which service boundary should the separate UI use? -> A: A separately deployed BFF service must mediate between the UI service and the core backend.
- Q: Where must the three services run? -> A: The UI, BFF, and core backend must all run on Azure, using a shared AKS platform while retaining independent deployment lifecycles.
- Q: How should existing machine consumers authenticate to the core backend? -> A: Use Microsoft Entra client-credentials tokens with dedicated core API app roles.
- Q: What expiry policy should apply to authenticated BFF browser sessions? -> A: Use a 30-minute idle timeout and an 8-hour absolute limit.
- Q: What retention policy should apply to employee learning records? -> A: Retain records while employed, then delete identity and personalized domain records within 90 days after departure; only fully unlinked security evidence may be irreversibly anonymized.
- Q: How should the system recognize that an employee has departed? -> A: Use Microsoft Entra account disablement or deletion, checked at sign-in and by daily reconciliation.
- Q: How should concurrent or repeated state-changing submissions be resolved? -> A: Require an idempotency key and make the first valid submission authoritative.
- Q: Which continuous-integration system must execute the delivery workflow? -> A: Jenkins.
- Q: Which Azure container service runs Jenkins agents in the configured resource group? -> A: Ephemeral Azure Container Instances agents.
- Q: How are ACR publication and AKS deployment privileges separated for Jenkins agents? -> A: Separate publisher and deployer ACI templates use distinct user-assigned managed identities.
- Q: How do ephemeral ACI agents connect to the local Jenkins controller? -> A: The controller's existing Jenkins Azure cloud-node configuration provisions and connects them.
- Q: What is the configured Jenkins Azure cloud-node name? -> A: `azure`.
- Q: How does Jenkins cloud `azure` authenticate to provision ACI agents? -> A: An existing service-principal credential is configured in the Jenkins controller.

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Create a Career Roadmap (Priority: P1)

An employee uses a browser-based interface to describe their current role, experience, target role, available learning time, and relevant preferences. The employee reviews the information, submits it, and receives a clearly structured career roadmap without needing to understand or call service endpoints directly.

**Why this priority**: Roadmap creation is the primary value of the career agent and provides a complete, independently useful pre-release MVP increment.

**Independent Test**: A tester can open the application, complete the roadmap form with valid employee information, submit it, and read the resulting milestones and recommendations entirely through the UI.

**Acceptance Scenarios**:

1. **Given** an organizational employee opens the application, **When** they complete Microsoft Entra ID sign-in and view the landing page, **Then** they can identify the product purpose and begin creating a career roadmap.
2. **Given** an employee enters valid profile information, **When** they submit the roadmap request, **Then** the interface shows progress and presents the completed roadmap in a readable structure.
3. **Given** required information is missing or invalid, **When** the employee attempts to continue, **Then** the interface identifies each affected field and explains how to correct it without discarding valid entries.
4. **Given** roadmap generation fails, **When** the failure is returned or the request cannot complete, **Then** the employee sees a plain-language message and can retry without re-entering the full profile.
5. **Given** the UI is available but the backend service is unavailable, **When** an employee attempts to create a roadmap, **Then** the UI identifies the temporary service outage and preserves the employee's valid entries for retry.

---

### User Story 2 - Explore Skill Guidance (Priority: P2)

An employee selects or enters a DevOps topic and receives focused guidance that is appropriate for their profile. The employee can move between their roadmap and skill guidance without losing the current results during the active browser session.

**Why this priority**: Topic guidance turns a broad roadmap into actionable learning steps and reuses employee context already supplied through the primary journey.

**Independent Test**: A tester can enter a supported topic and employee profile, submit the request, and review the returned explanation and recommendations without first creating a roadmap.

**Acceptance Scenarios**:

1. **Given** an employee provides a supported topic and valid profile, **When** they request guidance, **Then** the interface presents the guidance with a clear topic heading and actionable content.
2. **Given** an employee has already entered profile information in the active session, **When** they open skill guidance, **Then** the interface reuses that information while allowing edits before submission.
3. **Given** a topic is unavailable or cannot be interpreted, **When** the employee submits it, **Then** the interface explains the issue and preserves the entered profile and topic for correction.
4. **Given** guidance includes practical experience, **When** the employee reviews the recommended lab, **Then** they can see its provider, objective, prerequisites, estimated duration, cost status, and external destination before opening it.

---

### User Story 3 - Complete a Focused Learning Session (Priority: P2)

An employee completes a short learning session that combines a clear objective, concise learning steps, a practical lab reference when appropriate, and a review that confirms understanding. The employee receives immediate feedback and can retry missed questions before moving on.

**Why this priority**: Focused sessions convert recommendations into repeatable learning activity and provide the progress signals that encourage continued learning.

**Independent Test**: A tester can start one learning session, follow its ordered content, open a lab reference, answer 3-5 review questions, receive explanations, retry missed questions, and complete the session by reaching at least 80%.

**Acceptance Scenarios**:

1. **Given** an employee reaches the review step, **When** the review is displayed, **Then** it contains between 3 and 5 scored questions related to the session objective.
2. **Given** an employee submits a review, **When** an answer is evaluated, **Then** the interface immediately identifies whether it was correct and explains the correct reasoning.
3. **Given** an employee scores below 80%, **When** results are shown, **Then** the session remains incomplete and the employee can retry missed material and questions.
4. **Given** an employee scores at least 80%, **When** the review completes, **Then** the learning session is marked complete and progress is updated.
5. **Given** a learning session is completed, **When** completion is recorded, **Then** the employee receives a concise celebration of the milestone and exactly one recommended next action selected according to FR-034.

---

### User Story 4 - Record and Review Progress (Priority: P3)

An employee records a progress check-in against a roadmap and receives an updated review that highlights completed work, gaps, and exactly one prioritized recommended next action.

**Why this priority**: Progress review supports continued use after the initial plan but depends on an employee already having roadmap context.

**Independent Test**: Using an existing roadmap identifier, a tester can enter a progress update, submit it, and view the returned review and exactly one prioritized next action through the UI.

**Acceptance Scenarios**:

1. **Given** an employee has a valid roadmap reference, **When** they submit a progress check-in, **Then** the interface shows the review, current status, and exactly one prioritized recommended next action.
2. **Given** the roadmap reference is missing or unknown, **When** the employee submits a check-in, **Then** the interface clearly explains that a valid roadmap is required and preserves the entered progress notes.
3. **Given** a progress review is displayed, **When** the employee navigates to another UI area and returns during the active session, **Then** the latest review remains available.
4. **Given** an employee views their progress, **When** completed and upcoming work is shown, **Then** they can identify achieved milestones and exactly one recommended next action selected by this precedence: retry failed material, resume an active session, continue the next incomplete milestone, then review or refresh a completed roadmap.

### Edge Cases

- The interface remains usable when a user submits by keyboard rather than pointer input.
- Repeated submission controls are disabled while a request is in progress so one user action does not create duplicate requests.
- Long roadmap or guidance results remain readable and do not hide navigation or actions.
- Unexpected or partially structured error details are replaced with a safe, understandable message while preserving user-entered values.
- A browser refresh discards only UI state that the BFF has not confirmed as persisted and displays a notice identifying that unsaved state was discarded. After the authenticated BFF session is restored, the UI reloads the employee's persisted roadmap, learning, review, and progress records through the BFF.
- If an employee leaves before a learning session is complete, completed steps remain recorded and the employee can resume from the next incomplete step.
- Narrow screens reflow content without horizontal scrolling for primary forms, results, and navigation.
- If the service is temporarily unavailable, the UI distinguishes the outage from user input errors and provides a retry action.
- If an external lab link is unavailable, the employee can report the link and continue the learning session without losing progress.
- If lab validation is throttled or the provider returns an indeterminate temporary failure, the system records the attempt and retains the previous publication state until the configured consecutive-failure threshold is reached.
- If an employee leaves during a review, submitted answers and the incomplete session state are retained without incorrectly marking the session complete.
- If the UI deployment is unavailable, backend service health checks and existing machine consumers continue operating independently.
- If the UI and backend are temporarily on incompatible releases, the UI stops unsupported actions, shows a safe availability message, and does not corrupt saved progress.
- If the configured backend destination is missing or invalid, the UI fails closed with a clear operational error rather than sending personalized data to an unapproved destination.
- If a browser attempts to bypass the BFF and call a personalized backend capability directly, the backend rejects the request unless it carries independently valid authorization intended for that backend capability.
- If the UI is available but the BFF is unavailable, the UI preserves safe unsaved input, disables dependent operations, and presents a retryable service-unavailable state.
- If the BFF is unavailable, existing core-backend health checks and supported machine consumers continue operating independently.
- If a machine consumer presents a delegated employee token, browser session,
  missing app role, or token for the wrong audience, the core rejects the
  request without treating it as an approved machine identity.
- If a BFF session is idle for 30 minutes or reaches 8 hours from sign-in, the
  BFF revokes it and requires sign-in again without deleting saved progress.
- If an employee departs, their personalized roadmap, progress, and review
  history becomes ineligible for normal access and their identity plus all
  personalized domain records are deleted within 90 days; only security evidence
  with every direct and indirect owner link removed may be irreversibly anonymized.
- If the scheduled four-hour Entra reconciliation (stricter than the daily
  minimum) finds an account disabled or deleted, the system revokes active BFF
  sessions and blocks personalized access even when the employee does not
  attempt another sign-in.
- If retries, multiple tabs, or concurrent clients repeat a state-changing
  operation with the same idempotency key and payload, the system returns the
  first valid result without creating duplicate state; reuse with a different
  payload is rejected as a conflict.
- If Jenkins or an Azure delivery dependency becomes unavailable before the
  first environment mutation, the release stops, reports the failed stage, and
  leaves all deployed digests unchanged. If the dependency becomes unavailable
  after one or more services have changed, the release restores every service
  changed by that delivery attempt in reverse deployment order and verifies the
  pre-attempt digests before it terminates.
- If the existing Jenkins Azure cloud-node configuration cannot provision or
  connect an ACI agent, the build stops before Azure authentication or
  publication and does not expose the local controller or create a fallback
  long-lived Azure credential.

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: The complete four-journey pilot release MUST provide a browser landing page that explains the application purpose and offers direct navigation to roadmap creation, skill guidance, focused learning sessions, and progress check-ins. At an independently demonstrated pre-release story checkpoint, the landing page MUST expose only the journeys implemented by that checkpoint, MUST NOT present an unimplemented journey as available, and MUST label the build as a non-release increment.
- **FR-002**: The system MUST provide a roadmap form covering the employee information required by the existing career-roadmap workflow. UI-to-BFF field names, types, required fields, numeric bounds, constraints, and request shapes for roadmap, guidance, progress, learning, review, and lab-report operations MUST be generated from `contracts/bff-api-v1.openapi.yaml`; the BFF-to-core mapping MUST conform to `contracts/core-api-v1.openapi.yaml`.
- **FR-003**: The UI and BFF MUST validate browser-boundary requests against `contracts/bff-api-v1.openapi.yaml`, and the BFF and core MUST validate core-boundary requests against `contracts/core-api-v1.openapi.yaml`; core domain validation remains authoritative. The UI MUST place correction guidance beside or clearly associate it with each invalid field, and schema-validation failure MUST NOT consume an idempotency key or invoke domain work.
- **FR-004**: The system MUST show a visible in-progress state for every submitted operation and prevent duplicate submissions until the operation completes or fails.
- **FR-005**: The system MUST present roadmap results with distinguishable sections for the employee goal, milestones, learning recommendations, and other returned guidance.
- **FR-006**: The system MUST provide a skill-guidance form that accepts a topic and the employee profile information required by the guidance workflow. Accepted canonical topic identifiers and any allowed aliases MUST come only from the authoritative versioned catalog in `contracts/supported-guidance-topics-v1.yaml`; the UI and BFF MUST NOT invent or independently maintain supported topics, and the core MUST reject a topic absent from the active catalog version without consuming an idempotency key.
- **FR-007**: The system MUST present skill guidance with the requested topic, explanatory content, and actionable recommendations clearly distinguished.
- **FR-008**: The system MUST provide a progress check-in form that accepts a roadmap reference and the progress information required by the progress-review workflow.
- **FR-009**: The system MUST present a progress review with current status, identified gaps, and exactly one prioritized recommended next action selected according to FR-034.
- **FR-010**: The system MUST preserve valid form entries after validation, service, or connectivity errors so users can correct or retry without starting over.
- **FR-011**: The system MUST preserve the latest entered profile and displayed results while users navigate among UI areas during the active browser session. On browser refresh, the UI MUST discard only state that the BFF has not confirmed as persisted, display a notice that identifies the discarded unsaved state, restore the authenticated BFF session when still valid, and reload the employee's persisted roadmap, learning, review, and progress records through the BFF before presenting them as current.
- **FR-012**: The system MUST provide clear success, validation, not-found, service-unavailable, and unexpected-error feedback without exposing internal diagnostic details. Every US1-US4 success, validation/not-found, authentication-expiry, compatibility, dependency, retry, navigation, refresh, interruption, and intentional-not-applicable outcome MUST follow the primary-journey and stable dependency matrices in `contracts/implementation-readiness-contract.md`.
- **FR-013**: The system MUST make all primary journeys operable by keyboard and expose meaningful labels, headings, status changes, and error associations to assistive technologies. Logical focus order/targets/restoration, timeout warning/actions, external-new-context text, non-color progress/review semantics, and live-region behavior MUST meet the accessibility matrix in `contracts/implementation-readiness-contract.md`.
- **FR-014**: The system MUST remain usable on supported desktop, tablet, and mobile-width screens without horizontal scrolling for primary content. Verification MUST cover the frozen latest-two-major browser matrix, portrait/landscape orientation, 200% browser zoom/text scaling, keyboard, NVDA with Chrome/Firefox, and VoiceOver with Safari as defined by `contracts/implementation-readiness-contract.md`.
- **FR-015**: The system MUST provide a consistent way to return to the application landing page. A pre-release story checkpoint MUST offer switching only among journeys whose story checkpoint has passed and MUST keep every other journey absent from navigation and direct entry; the complete four-journey pilot release MUST offer switching among all four primary journeys.
- **FR-016**: The system MUST preserve the documented request semantics, response schemas, health behavior, and business outcomes used by existing machine consumers. `contracts/core-api-v1.openapi.yaml` is the authoritative versioned definition of core API request and response schemas, `contracts/bff-api-v1.openapi.yaml` is authoritative for the UI-to-BFF boundary, `contracts/supported-guidance-topics-v1.yaml` is authoritative for supported topics, and `contracts/implementation-readiness-contract.md` is authoritative for the cross-journey failure, lifecycle, measurement, accessibility, browser, recovery, and prerequisite decisions it enumerates. `requirements-traceability.md` MUST contain one complete implementation/verification/evidence row for every FR-001 through FR-075 and SC-001 through SC-050. A conflict, missing or empty row, duplicate or out-of-order requirement key, unknown task, or drift between implementation, generated consumers, UI validation, prose, or any authoritative artifact MUST block implementation and release until corrected. Introducing Microsoft Entra authentication, private routing, and pre-idempotency rejection of guidance topics absent from the active catalog are intentional compatibility changes, and anonymous access or uncatalogued-topic fallback MUST NOT be preserved.
- **FR-017**: The system MUST avoid displaying secrets, internal stack traces, or sensitive configuration values in UI content or browser-visible errors.
- **FR-018**: The system MUST clearly identify information retained only in the active browser session with a persistent `Not yet saved` indicator and MUST remove that indicator only after the BFF confirms successful persistence. A refresh MUST never present BFF-unconfirmed state as restored or saved; persisted state reloaded through the BFF MUST replace any stale browser copy.
- **FR-019**: The system MUST require employees to authenticate with their organizational Microsoft Entra ID identity before accessing personalized learning information.
- **FR-020**: The system MUST associate saved roadmaps, progress, and learning activity with the authenticated employee and prevent employees from viewing another employee's information.
- **FR-021**: The system MUST provide a clear sign-out action and end access to personalized information when sign-out completes or the authenticated session expires.
- **FR-022**: The system MUST organize recommended learning into focused sessions designed to take 20-30 minutes and show the estimated duration before an employee starts. The required-content clock MUST start/stop/pause and exclude optional material, external-lab time, interruptions, and retries exactly as defined in `contracts/implementation-readiness-contract.md`.
- **FR-023**: The system MUST show progress within the current learning session and persist completed steps for the authenticated employee.
- **FR-024**: The system MUST allow an employee to leave a learning session and later resume from the next incomplete step.
- **FR-025**: The system MUST include curated external hands-on lab references where practical experience supports the learning objective.
- **FR-026**: Each lab reference MUST identify its provider, objective, prerequisites, estimated duration, cost status, and external destination before the employee opens it.
- **FR-027**: The system MUST clearly distinguish external lab content, preserve application progress when employees leave for a lab, and allow employees to report unavailable or unsuitable links.
- **FR-028**: Each learning session MUST include a review containing between 3 and 5 scored questions aligned with the pinned session objective/content version; a content deployment MUST NOT mix versions within an attempt.
- **FR-029**: The system MUST provide correctness feedback and a concise explanation after each submitted review answer and commit it to an announced accessible region within one second of parsing the complete valid BFF response.
- **FR-030**: The system MUST require a score of at least 80% to mark a learning session complete and MUST allow employees below the threshold to review missed concepts and retry a new full attempt over the same pinned questions. There is no lifetime attempt cap, but no more than five attempts may start in 60 minutes; the first passing attempt permanently establishes completion.
- **FR-031**: The system MUST retain immutable review attempts, answers, scores, completion status, and timestamps for the authenticated employee's progress history; history MUST identify the latest attempt and highest score without allowing a later lower score to reverse completion.
- **FR-032**: The system MUST present learning progress as visible roadmap and session milestones that distinguish completed, current, and upcoming work.
- **FR-033**: The system MUST acknowledge completed milestones with concise positive feedback and MUST present exactly one clearly prioritized recommended next action in learning-completion and progress-review responses and displays.
- **FR-034**: The core MUST deterministically select exactly one recommended next action from the employee's roadmap, completed sessions, review outcomes, and incomplete work by applying this precedence: (1) retry failed material, (2) resume an active learning session, (3) continue the next incomplete roadmap milestone, and (4) review or refresh the completed roadmap when no incomplete learning work remains. Within the first applicable class, ties MUST resolve by lowest roadmap milestone ordinal, then oldest unresolved activity time, then stable identifier. The BFF MUST preserve the core selection without re-ranking, the UI MUST display only that selection, and no competitive ranking may be introduced.
- **FR-035**: The UI, BFF, and core backend MUST each be packaged, deployed, configured, scaled, updated, rolled back, and monitored as independent services.
- **FR-036**: The UI MUST access personalized career-roadmap, guidance, progress, learning, and identity-scoped capabilities only through the BFF and MUST NOT directly access core-backend interfaces or storage.
- **FR-037**: The core backend MUST remain independently available to approved machine consumers and private health monitoring while either the UI or BFF is unavailable, deploying, or rolling back.
- **FR-038**: The UI MUST use environment-specific BFF destination and trust configuration, and the BFF MUST use environment-specific core-backend destination and trust configuration, without requiring source changes or environment-specific rebuilds.
- **FR-039**: The UI, BFF, and core backend MUST detect unsupported interface-version combinations at their respective boundaries. The BFF MUST expose its contract version, active BFF API schema version, accepted UI contract range, and current core-compatibility state through a capabilities resource; state-changing domain requests from the UI MUST identify the UI contract version. Session logout is deliberately exempt from compatibility gating so an active session can always be revoked after its CSRF and exact-Origin checks pass, while an invalid/already-revoked-session retry after exact-Origin validation can still clear the cookie without regaining authority. State-changing delegated BFF-to-core requests MUST identify the BFF contract version, and the core MUST expose its contract version and accepted BFF range. The BFF-specific version header MUST remain optional and nonsemantic for approved app-role machine consumers so their documented `/api/v1` request shapes remain compatible. Either UI/BFF or BFF/core boundary MUST reject an unsupported state-changing domain request with `409 CONTRACT_VERSION_UNSUPPORTED` before invoking downstream work, consuming an idempotency key, or changing state, while the UI preserves valid unsaved input and presents a safe retry or availability message. Capability metadata MUST refresh at 45 seconds, MUST be no older than 60 seconds to enable a mutation, MAY be used for labelled read-only diagnosis only through five minutes, and MUST fail closed with the stable invalid/unavailable codes in `contracts/implementation-readiness-contract.md`.
- **FR-040**: Authentication boundaries MUST prevent reusable credentials from reaching the UI and MUST prevent personalized records from reaching unapproved origins or untrusted destinations. The UI MAY receive only the authenticated employee's authorized, presentation-safe personalized records through the same-origin BFF.
- **FR-041**: As the BFF-specific refinement of the three-service rule in FR-035, the BFF MUST run as an independently addressable AKS workload with its own image, runtime configuration, health signal, scaling policy, deployment history, and rollback lifecycle; it MUST NOT share a process or deployment unit with the UI or core backend.
- **FR-042**: The BFF MUST be the only UI-facing service authorized to invoke personalized core capabilities. Core authorization MUST reject browser cookies, untrusted forwarded-identity headers, and tokens not issued for the core API.
- **FR-043**: The BFF MUST own browser-specific concerns, including sign-in initiation, browser session handling, request validation, user-facing response shaping, and safe translation of backend failures.
- **FR-044**: The BFF MUST call the core through its versioned private API and attach an access token intended specifically for the core API.
- **FR-045**: The core MUST derive employee ownership only from independently validated token claims and MUST ignore browser-provided identity fields and forwarded identity headers.
- **FR-046**: The BFF MUST avoid duplicating career-roadmap, learning-progress, review-scoring, next-action, and persistence business rules owned by the core backend.
- **FR-047**: The UI service MUST remain presentation-focused and MUST NOT own BFF session mediation, core-backend credentials, authoritative authorization decisions, or core business rules.
- **FR-048**: Each service MUST expose an independent process liveness and dependency-aware readiness signal that distinguishes UI availability, BFF availability, core-backend availability, and per-replica trust/configuration convergence. The exhaustive gateway/UI/BFF/core/Redis/PostgreSQL/Entra/JWKS/Key-Vault/ACR/AKS/Monitor/lab/Jenkins/ACI/evidence failure, retry, atomicity, recovery, ownership, and evidence rules are normative in `contracts/implementation-readiness-contract.md` and `contracts/auth-ui-contract.md`.
- **FR-049**: The UI, BFF, and core backend MUST all be hosted on Microsoft Azure and deployed as independent workloads on the shared AKS application platform.
- **FR-050**: Deployment MUST use Azure-managed platform capabilities for container images, ingress, secrets, session storage, relational persistence, identity, and observability where doing so reduces application operational burden.
- **FR-051**: The deployment topology MUST expose only the UI and BFF through the approved Azure-managed gateway; the core backend and data services MUST remain private.
- **FR-052**: A release MUST promote immutable UI, BFF, and core image digests independently through ACR and MUST support compatibility-checked progressive rollout and rollback without rebuilding images. If a multi-service delivery attempt fails, is aborted, or loses a required Jenkins or Azure dependency after an environment mutation, every service changed by that attempt MUST be restored to its pre-attempt digest in reverse deployment order; services not changed by that attempt MUST remain untouched.
- **FR-053**: The UI MUST use an opaque BFF session and MUST NOT receive, store, or forward Microsoft Entra access, ID, or refresh tokens.
- **FR-054**: The BFF MUST be registered as a confidential web client, use the authorization-code flow, and request only the delegated core API scopes required for the signed-in employee. Independently of an employee session, the same BFF registration MUST use its certificate to obtain an app-only token containing only `CareerAgent.Health.Read` for private core capability and readiness reads; that token MUST NOT authorize personalized operations.
- **FR-055**: The core MUST independently validate delegated access-token signature and exact algorithm/key source, issuer, tenant, audience, `nbf`/`exp` with at most five-minute skew, authorized `azp`/`appid` client, required `scp`, token type, and employee `(tid, oid)` claims before authorizing personalized operations. JWKS refresh/staleness and every denial MUST follow the delegated-token matrix in `contracts/auth-ui-contract.md`.
- **FR-056**: BFF, core, lifecycle-reconciliation, retention, lab-revalidation, migration-Job, evidence-hold-reconciler, ALB-controller, and gateway-certificate/DNS workloads MUST use separate Microsoft Entra Workload Identities with least-privilege access to their Azure dependencies; ACR image pull MUST remain assigned to the AKS kubelet identity.
- **FR-057**: Azure Managed Redis and Azure Database for PostgreSQL connections MUST use Microsoft Entra token authentication in deployed Azure environments, with token refresh and reconnect behavior verified under expiry and transient failure.
- **FR-058**: Approved machine consumers MUST authenticate directly to the core using Microsoft Entra client-credentials access tokens containing dedicated core API application roles; browser sessions and employee-delegated scopes MUST NOT grant machine-consumer access. Each request MUST independently pass the exact signature/JWKS/algorithm, issuer/tenant, audience, lifetime, approved `azp`/`appid`, operation-role, absent-`scp`, wrong-token-type, and application-owner allow/deny matrix in `contracts/auth-ui-contract.md`. Stateful machine roadmap and progress operations MUST create or mutate only application-owned records keyed by the validated machine principal, MUST NOT select or impersonate an employee owner, and MUST reject cross-application or employee-owned references. Machine guidance MAY remain stateless but MUST use the application principal for audit and idempotency.
- **FR-059**: Authenticated BFF browser sessions MUST expire after 30 minutes without activity and MUST expire no later than 8 hours after sign-in regardless of activity; expiry MUST revoke server-side session access and preserve already saved learning records.
- **FR-060**: The system MUST delete departed employees' identity, profile, roadmap, progress, learning, review, report, and idempotency records within 90 days. Security evidence MAY be irreversibly anonymized only after all direct and indirect employee links are removed, and aggregate telemetry MAY remain only when it cannot be reidentified.
- **FR-061**: The system MUST recognize departure from Microsoft Entra account disablement or deletion, enforce the status during sign-in, reconcile active employee identities at least daily using a first-release four-hour schedule, and revoke active browser sessions within 24 hours of the directory change. The dedicated lifecycle identity MUST use only administrator-consented Microsoft Graph application permission `User.Read.All` for those read-only known-user checks. Marking departure MUST atomically enqueue both a durable owner-revocation outbox record and an unclaimed retention action before the reconciliation checkpoint advances. The lifecycle database role MAY insert or narrowly update only unclaimed retention-scheduling fields and MUST NOT select, delete, claim, or complete retention rows. A dispatcher MUST send the authenticated command, carrying no reusable credential or display data, to a private BFF route protected by `LearningBff.Session.Revoke`; that route MUST NOT be exposed by the browser gateway. Delivery MUST retry until idempotent BFF acknowledgement, alert by 2 hours, page by 6 hours, and reach a 12-hour post-recognition deadline, leaving at least 8 hours of end-to-end margin. No successful reconciliation for 6 hours MUST warn Application and Platform Operations; 8 hours MUST page both.
- **FR-062**: Every retryable domain mutation identified by the BFF/core OpenAPI contracts MUST require and propagate an actor-and-operation-scoped idempotency key. After authentication, compatibility, and validation, the first valid submission MUST atomically claim `processing` with its canonical request hash, a 60-second execution lease, and a heartbeat at least every 15 seconds. An identical replay during a live lease MUST return `409 IDEMPOTENCY_IN_PROGRESS` with `Retry-After: 2`; a different hash MUST return `409 IDEMPOTENCY_KEY_REUSED` without changing state. After process restart or lease expiry, exactly one recovery worker MUST inspect the durable operation/outbox/resource reference, finalize an already committed result, or reacquire the same key and execute only when no transition committed; domain transition and `succeeded` result MUST commit atomically. `retryable_failed` MUST certify that no domain mutation committed and MAY reacquire only the same key after its recorded retry time; `final_failed` MUST replay its established failure and MUST never execute again. A caller timeout MUST be resolved by same-key replay and MUST NOT generate a replacement key. Full response bodies MUST remain replayable for 30 days; after that, a permanent non-reusable actor/operation/key/hash/status/resource tombstone MUST remain for the actor's remaining lifetime and same-key replay MUST return `409 IDEMPOTENCY_RESULT_EXPIRED` without executing. Employee tombstones MAY be removed only with the complete FR-060 owner purge, and machine tombstones only 90 days after principal revocation. Authentication lifecycle routes remain explicit exceptions: logout is intrinsically idempotent because repeated calls converge on no active session/cookie, and delegated session bootstrap is semantically idempotent by validated tenant/subject and may only establish the same eligibility/status result. Neither exception may create duplicate domain records or broaden authority.
- **FR-063**: Before publication, every lab reference MUST pass HTTPS, approved-provider-domain, redirect-chain, metadata-completeness, and reachability validation. Validation MUST allow at most five redirects and MUST reject a repeated-URL loop, HTTPS-to-HTTP downgrade, non-approved destination, or private, link-local, metadata, cluster, or other internal target as a final policy failure. A dedicated lab-revalidation CronJob MUST start every 20 hours with `concurrencyPolicy: Forbid`, a bounded deadline, and a distinct Workload Identity/database role limited to lab-reference validation state. Active references MUST thereby be revalidated at least once every 24 hours using a 10-second request timeout and no more than two retries. Its default-deny NetworkPolicy MUST allow PostgreSQL/Entra/DNS and public HTTPS only while application redirect/hostname checks still enforce the exact recorded provider policy version. A validation age over 30 hours MUST warn Learning Content Operations; over 36 hours MUST page Learning Content and Application Operations. A reference MUST become unavailable after three consecutive failed scheduled validations, which MUST alert Learning Content Operations immediately, and MAY return to active only after a later complete validation succeeds. Provider/domain approval removal MUST immediately make affected references unavailable and require a new policy approval plus complete validation for recovery. An audited Learning Content Operations retirement MUST stop new publication, external opens, and scheduled validation while preserving learner progress, reports, and history; reversal MUST create and validate a new reference version rather than mutate the retired version. Reports MUST be append-only, queue out-of-cycle validation within 15 minutes, and use row-version/transactional concurrency with validation so neither a report nor validation state can overwrite the other.
- **FR-064**: The Jenkins controller at `http://localhost:8080` MUST orchestrate continuous integration and delivery through the existing Jenkins Azure cloud node named `azure`, whose existing service principal is authorized only to provision and connect ephemeral Azure Container Instances agents in the configured resource group and attach their approved identities. Non-Azure checkout, contract drift, lint, typecheck, and tests for every ref, including protected refs, MUST run on `azure-aci-validator`; pull-request and unprotected-ref jobs MUST be confined to it. The validator MUST have no Azure managed identity and MUST be unable to authenticate to Azure, publish images, read delivery evidence, promote, deploy, verify an environment, or roll back. Publication stages MUST run only on the publisher ACI template using its ACR-scoped user-assigned managed identity; protected live-manifest/preflight checks, promotion, migration-Job orchestration, deployment, verification, and rollback MUST run only on the deployer ACI template using its AKS-scoped user-assigned managed identity and control-plane Reader limited to the target/configured resource group. The deployer MUST NOT read Terraform state, Key Vault secret values, ACR content, Redis data, or PostgreSQL data. The controller service principal MUST NOT receive ACR push or AKS deployment permission, and Jenkins MUST store no additional Azure application-delivery credential. `contracts/jenkins-delivery-contract.md` is normative for all delivery paths: only a configured Delivery Recovery Operator may request manual rebuild/recovery and a distinct Platform Operations member MUST approve; its event-specific notification routes and acknowledgement/escalation deadlines MUST apply; each reversible compensation is limited to three attempts at 0/15/45 seconds within a 20-minute total rollback deadline, after which `rollback_failed`, quarantine, and two-person recovery approval are mandatory; controller restart/disk-loss reconciliation MUST recover accepted attempts without duplicate promotion; and a published-but-unpromoted digest MUST remain quarantined, MUST NOT be selected by another build without a new release manifest and every current gate, and becomes cleanup-eligible only after 30 days when unreferenced and unheld.
- **FR-065**: Learning Content Operations MUST own and audit the lab-applicability classification for each content version. A published focused learning session MUST include a hands-on lab when its objective requires command execution, configuration, deployment, troubleshooting, or observation of a running system. A lab MAY be omitted only for orientation, conceptual comparison, or review-only objectives and only when the allowed reason, classifier identity, decision time, and policy version are recorded with the content version. Any borderline or mixed objective MUST default to requiring a lab; Security Reviewers govern provider/domain trust but MUST NOT override applicability.
- **FR-066**: Lab destinations MUST belong to a versioned provider allowlist. Every addition or domain expansion MUST receive approval from two distinct human identities: one member of the configured Learning Content Owners Microsoft Entra group and one member of the configured Application Security Reviewers Microsoft Entra group. One identity MUST NOT satisfy both approvals, including when that person belongs to both groups. The change record MUST contain provider, domains, both approver identities, reason, effective time, policy version, and audit reference. The initial allowlist MUST be empty.
- **FR-067**: Jenkins cloud `azure` MUST bind the `azure-aci-publisher` template exclusively to the publisher user-assigned managed identity and the `azure-aci-deployer` template exclusively to the deployer user-assigned managed identity. The `azure-aci-validator` template MUST bind no Azure managed identity. A missing, additional, or mismatched publisher/deployer identity, or any system-assigned or user-assigned identity on the validator template, MUST stop the pipeline before Azure authentication or untrusted validation execution.
- **FR-068**: BFF confidential-client certificate rotation MUST introduce the replacement certificate at least 24 hours before retiring the previous certificate. During normal rotation, the previous certificate MUST remain valid until every BFF replica has loaded the replacement and successful core-token acquisition has been demonstrated; existing browser sessions and new sign-ins MUST continue without forced reauthentication, and the previous credential MUST be removed within 48 hours of replacement activation. A suspected compromise MUST trigger immediate revocation without waiting for the overlap period; emergency rotation MAY require affected employees to reauthenticate, but MUST preserve already saved learning records and present a safe sign-in recovery path.
- **FR-069**: The Jenkins cloud `azure` provisioning service principal MUST be owned by the platform operations team and rotated at least every 90 days. Platform operations MUST receive credential-expiry alerts at 30, 14, and 7 days before expiry. The credential MUST be replaced before fewer than 30 days of validity remain. Only a dedicated least-privilege Jenkins credential-manager role, usable through the controller's localhost administrative interface and restricted to the stable `azure` cloud credential entry, MAY install a replacement credential. Replacement secret material MUST NOT appear in command arguments, environment variables, repository files, retained temporary files, or logs. Normal rotation MUST prove identityless validator, publisher, and deployer ACI agent provisioning before the previous credential is revoked. Suspected compromise MUST trigger immediate revocation, disable ordinary cloud-agent provisioning, and permit only an audited rotation-validation path until all three declared template checks pass.
- **FR-070**: Jenkins delivery evidence MUST be retained for 90 days in the non-production environment. Authorized readers MUST be derived from separately configured Delivery Operators and Security Reviewers Microsoft Entra group object IDs. Incident-hold creation, extension, and release MUST be restricted to a separately configured Evidence Hold Managers Microsoft Entra group and MUST be audited; evidence-reader membership alone MUST NOT grant hold-management authority. Publisher and deployer workload identities MAY create evidence only within their assigned immutable paths and MUST NOT receive general read, list, tag-mutation, legal-hold, or deletion permission. Path isolation MUST use Azure role-assignment conditions scoped to the assigned environment and delivery-stage prefix. Every upload MUST use `If-None-Match: *`, and an accepted evidence version MUST receive a locked fixed 90-day version-level time-based immutability policy so that no writer can successfully replace or delete it even if the conditional header is omitted or bypass is attempted. An incident hold MUST set the Azure version-level legal-hold boolean on every enumerated `blob_version_id` in the evidence set and MUST record a separate audited hold ID, owner, reason, incident reference, start, target-version inventory, and expiry no later than 180 days from creation. Set/clear is logically all-or-fail: partial Azure success MUST enter a reconciling state, alert, and retry until every target version matches before the hold is declared active or released. A distinct hourly hold-reconciler Workload Identity MUST read only the hold-control inventory and set/clear only those exact version holds; it MUST NOT read/list/delete blob content or change the fixed policy. Expiry or authorized release clears each version-level hold but MUST NOT shorten the fixed 90-day lock; evidence becomes deletion-eligible only when `immutable_until` has elapsed and no version hold remains. Tokens, kubeconfigs, secrets, and personal learning data MUST never be archived.
- **FR-071**: Azure infrastructure provisioning/import MUST be a Platform Operations prerequisite executed outside Jenkins with time-bounded PIM activation, a reviewed Terraform plan, Azure Storage remote state/blob-lease locking, pinned AzureRM/AzureAD providers and lock file, and no Terraform/operator/bootstrap secret in the repository or Jenkins controller. Permissions MUST be limited to state-container data access, app-resource-group contribution/RBAC administration, explicitly named shared network/DNS resources, allowlisted provider-registration/quota reads, declared application objects, and database bootstrap; Microsoft Graph application consent MUST use a separate just-in-time Privileged Role Administrator approval. `Owner`, `Global Administrator`, unrelated resource/app/data access, standing excess privilege, and any Jenkins principal are prohibited. The sole existing controller-held Jenkins cloud provisioning service-principal credential required by FR-064/FR-069 remains permitted and MUST NOT gain Terraform, state, or delivery access; no additional delivery credential may be stored. Bootstrap/apply and data-principal commands MUST NOT emit the environment manifest. A distinct finalization command MUST be the sole manifest emitter and MUST run only after all Terraform modules—including evidence lifecycle and monitoring—are applied, all principals and cross-denials are verified, and the ALB Controller plus cluster-admin-owned migration guardrails are live. The reviewed nonsecret manifest MUST contain state lineage/serial, final repository configuration digest, exact resources/identities/origins, controller/migration-policy attestations, assignment/activation/approver evidence, and provider/quota/capacity attestations that expire after seven days. The digest MUST use the versioned ordered input policy in `config/platform-configuration-digest-v1.yaml`: resolve allowlisted regular files from IaC, Kubernetes, provider locks, platform scripts/config schemas, and mandatory provision/teardown skill assets; sort normalized repository-relative UTF-8 paths bytewise; hash a canonical stream of path, executable mode, byte length, and SHA-256 content digest; reject symlinks/path traversal/unmatched required inputs; and include the policy file itself. It MUST explicitly exclude `config/platform-bootstrap-nonprod.json`, Terraform state/plan/cache/local `terraform.tfvars`, runtime evidence/logs, secrets, and VCS metadata so finalization is not self-referential. Matching add/remove/rename/mode/content drift MUST change or fail the digest. Jenkins MUST NOT run Terraform apply/import or read remote state and MUST fail protected delivery when that manifest or required resources are absent, expired, stale, or inconsistent.
- **FR-072**: Expand-only core migrations MUST run as a bounded, immutable-image-digest Job in a dedicated Kubernetes migration namespace, using a dedicated service account/Workload Identity and least-privilege PostgreSQL DDL/backfill role over private networking. The deployer MUST NOT receive database credentials or direct database permission. Namespace RBAC MUST permit only Job `create/get/watch/delete`, Pod `get/list/watch`, and `get` on `pods/log`, while denying exec, attach, port-forward, secret/configuration access, and service-account mutation. A cluster-admin-owned admission policy that the deployer cannot alter MUST enforce the exact migrator service account, one nonprivileged container, approved ACR core repository by digest, fixed migration runner, allowed explicit Alembic target, allowlisted environment/volumes, deadline/retry/TTL, and no host access. Platform Operations MUST install and attest the namespace, RBAC, policy, and binding during external finalization before protected delivery; Jenkins may verify but MUST NOT install or alter them. Publisher/deployer separation MUST prevent the identity selecting the digest from publishing it. The Job MUST publish identity/digest/target/before-and-after-head/status/log/policy/cleanup evidence and be cleaned up before core rollout or failure finalization.
- **FR-073**: The target topology MUST remove the legacy public core Ingress and AKS web-app-routing NGINX add-on, install a pinned Application Gateway for Containers ALB Controller with its declared Workload Identity/RBAC before Gateway resources, and expose only UI/BFF on the public TLS Gateway. Approved machine consumers MUST run in the AKS VNet or an explicitly peered/private-connected network and reach core through a separate Azure-internal L4 LoadBalancer/private-DNS endpoint with no public IP. Core MUST terminate HTTPS itself using a rotated Key Vault/CSI server certificate and HTTPS probes; source ranges/NSGs, private trust, NetworkPolicy, and app-role authorization MUST all be enforced. Public-gateway and private-core certificate rotation MUST follow `contracts/auth-ui-contract.md` as the normative lifecycle: normal rotation uses a validated disabled candidate, at least 24 hours of old/new overlap, complete AGC-frontend or core-plus-BFF-trust convergence, and verified rollback before old-version retirement; partial convergence removes affected endpoints/replicas from readiness, quarantines the candidate, retains the prior safe version, and requires explicit operator release; suspected compromise immediately removes the compromised version with no rollback to it and keeps service unavailable rather than downgrade TLS or use untrusted fallback. Public-gateway and private-core certificate expiry MUST warn Platform Operations at 30, 14, and 7 days and page critically below 48 hours. Deployment output MUST publish the authoritative AGC HTTPS browser URL and MUST NOT use the legacy NGINX address helper.
- **FR-074**: Azure data-plane bootstrap MUST create separate least-privilege Managed Redis BFF, PostgreSQL core-DML, lifecycle-known-identity/status/checkpoint/outbox plus unclaimed-retention-scheduling, retention-procedure-only, lab-validation-state-only, and migration-DDL/backfill principals/assignments. The lifecycle role MAY update only required status/timestamp columns on known identities and reconciliation/outbox records and insert or narrowly update only unclaimed `RetentionAction` scheduling fields; it MUST NOT select, delete, claim, or complete retention rows. The retention role MAY execute only audited security-definer claim/process-due procedures that delete the identity and complete personalized owner graph, remove every owner link, and only then irreversibly anonymize eligible unlinked security evidence; it has no direct queue or learning-row access. The lab role MAY read active lab-reference destinations/policy version and update only validation timestamps, status, failure counters, and safe error codes. Bootstrap MUST prove cross-role denial and disable access-key/password fallback after verification. Terraform provider configuration MUST explicitly support both Azure Resource Manager and Microsoft Entra resources with locked provider versions.
- **FR-075**: The pre-agent Jenkins controller audit MUST be created and finalized by an administrator-installed, pinned controller plugin using a `RunListener`/`RunAction` mechanism that requires no workspace or executor and runs independently of every repository Jenkinsfile or shared-library call. The installed artifact MUST come from an administrator-approved protected revision with a verified digest and MUST create `pending` state on run start before any ACI request even when repository code omits or attempts to bypass audit calls. Repository shell/shared-library tooling MAY request monotonic stage updates or synchronize the record after an agent starts but MUST NOT create, replace, finalize, or suppress controller state. Before requesting an ACI agent, the plugin MUST fsync every accepted lifecycle transition to both the Jenkins run record and a host-managed append-only replicated controller-audit store; unhealthy two-copy persistence MUST disable protected scheduling. Platform Operations MUST own that store, hourly integrity verification, and encrypted backup, providing zero accepted-transition RPO and at most four-hour controller-recovery time. After controller restart or disk loss, the plugin MUST restore from the replica, reconcile every build ID against queue/run metadata, source-control webhook audit, and Azure evidence, finalize accepted nonterminal orphans as `aborted_recovered`, and block new protected delivery until every post-mutation orphan has completed verified rollback or an approved recovery plan reaches a verified terminal state.

### Key Entities

- **Employee Profile**: Information describing the employee's current role, experience level, target role, available learning time, preferences, and constraints.
- **Career Roadmap**: A generated career-development plan associated with a profile and containing milestones and learning recommendations; browser journeys use an employee owner, while approved machine operations use the validated application owner.
- **Skill Guidance Request**: A selected DevOps topic combined with employee context for producing focused learning guidance.
- **Progress Check-In**: An actor's progress update associated with an existing roadmap owned by that same employee or approved application principal.
- **Progress Review**: The evaluation returned for a check-in, including current status, gaps, and exactly one prioritized recommended next action selected according to FR-034.
- **Active UI Session**: Temporary browser state containing form entries and the latest displayed results; it is not an additional permanent user record.
- **Employee Identity**: The organizational identity established through Microsoft Entra ID and used to scope access to the employee's saved learning information.
- **Learning Session**: A focused 20-30 minute unit containing a goal, ordered learning activities, progress state, and completion status.
- **Hands-On Lab Reference**: A curated external practical exercise with a provider, learning objective, prerequisites, expected duration, cost status, destination, and availability/reporting state.
- **Session Review**: A set of 3-5 scored questions tied to one learning session, including submitted answers, immediate explanations, attempt score, attempt time, and completion outcome.
- **Learning Milestone**: A meaningful roadmap or session achievement with completion state, completion time, and relationship to the employee's next recommended action.
- **Backend Service Interface**: The documented, versioned boundary through which the separately hosted UI requests authenticated roadmap, guidance, learning, and progress capabilities.
- **Backend for Frontend**: A service deployed separately from both the UI and core backend that manages UI-oriented sessions, request orchestration, response composition, and safe delegation to authoritative core-backend operations.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: The single exploratory PoC participant can submit a valid roadmap and identify its first recommended action without facilitator assistance on the first attempt.
- **SC-002**: In the complete four-journey pilot release, users can reach and begin any of the four primary journeys within two interactions from the landing page. Pre-release story checkpoints are assessed only against their explicitly implemented journeys under FR-001 and do not satisfy this release criterion.
- **SC-003**: For locally detectable input errors, users receive correction guidance within one second of attempting submission. Verification MUST follow `contracts/implementation-readiness-contract.md` §Reproducible one-second measurements: a production UI build on the defined 4-vCPU/8-GiB worker, the first submit-handler instruction as the start, accessibility-tree commitment of the error summary, associated field guidance, focus target, and status announcement as the stop, one fixture for every generated/documented validation constraint, five measured repetitions in each supported browser major after only the declared warm-up, and every crash, timeout, exception, or invalid/missing timing mark counted as failure.
- **SC-004**: For at least 95% of successful roadmap and skill-guidance requests, no more than one second elapses between resolution of the browser's BFF fetch with the complete validated success payload and commitment of the corresponding result to the rendered accessibility tree; both boundaries MUST be captured by browser performance marks. Verification MUST follow `contracts/implementation-readiness-contract.md` §Reproducible one-second measurements: 100 valid attempts per journey across ten isolated browser contexts after the declared warm-up, at least 95 successful payloads, nearest-rank p95 of at most 1,000 ms among successful payloads, fixed fixture and service digests, failures retained in the 100-attempt availability count, and missing/invalid marks on a successful payload counted as render failures.
- **SC-005**: All primary journeys can be completed using keyboard-only navigation with a visible focus indicator and no focus traps.
- **SC-006**: On the latest two stable major releases of Chrome, Edge, Firefox, and Safari/WebKit at verification time, primary navigation, forms, results, learning content, review questions, and progress displays remain readable without page-level horizontal scrolling at viewport widths of 320, 375, 768, 1024, 1440, and 1920 pixels; Chrome and Safari/WebKit mobile emulation also preserve touch operability at the mobile widths.
- **SC-007**: The single exploratory PoC participant rates the clarity of forms, progress feedback, results, and errors as 4 or higher on a predefined 5-point questionnaire.
- **SC-008**: After approved machine consumers adopt Entra client-credentials authentication and the private core route, their supported operations conform to `contracts/core-api-v1.openapi.yaml` and complete without request-semantic, response-schema, health, or business-behavior regressions; stateful results are application-owned by the validated principal, and guidance requests accept only topics or aliases defined by the active version of `contracts/supported-guidance-topics-v1.yaml`.
- **SC-009**: A failed request can be corrected or retried without re-entering valid information in 100% of tested validation, not-found, and temporary-outage scenarios.
- **SC-010**: In authorization testing, employees can access 100% of their own saved learning records and 0% of records belonging to another employee.
- **SC-011**: The single exploratory PoC participant completes the required content and first review attempt of a focused session within 20-30 minutes; optional material, external-lab time, interruptions, and retries are recorded separately.
- **SC-012**: In all tested interruption scenarios, employees can resume at the next incomplete session step without repeating a recorded completed step.
- **SC-013**: In content-quality review, 100% of displayed lab references include provider, objective, prerequisites, estimated duration, cost status, and a reachable destination at the time of publication.
- **SC-014**: In all tested learning sessions, scores below 80% remain incomplete and scores of 80% or higher update completion and progress correctly.
- **SC-015**: The single exploratory PoC participant rates the usefulness of review correctness feedback and explanations as 4 or higher on a predefined 5-point questionnaire.
- **SC-016**: The single exploratory PoC participant can identify the current milestone, completed milestones, and recommended next action without facilitator assistance.
- **SC-017**: The single exploratory PoC participant rates the statement "The completion feedback and recommended next action make me want to continue learning" as 4 or higher on a predefined 5-point questionnaire.
- **SC-018**: As the deployment measurement of FR-035, deployment verification shows that each of the UI, BFF, and core backend can be released and rolled back without rebuilding or redeploying either of the other two services.
- **SC-019**: During a simulated UI outage, 100% of existing backend health checks and supported machine-consumer scenarios continue to operate normally.
- **SC-020**: In compatibility testing, 100% of unsupported UI-to-BFF and BFF-to-core version combinations return `409 CONTRACT_VERSION_UNSUPPORTED` before a downstream call, idempotency-key consumption, or state mutation, while preserving valid user-entered information and saved progress; supported combinations advertise mutually compatible ranges plus the active BFF API schema, core API schema, and guidance-topic catalog versions through the required capabilities and version metadata.
- **SC-021**: In architecture verification, 100% of browser-originated personalized operations pass through the BFF, and direct unauthenticated or improperly targeted backend requests are rejected.
- **SC-022**: Contract tests demonstrate that changing UI presentation or BFF response composition does not require changes to core backend business rules for all four primary user journeys.
- **SC-023**: During a simulated BFF outage, 100% of existing core-backend health checks and supported machine-consumer scenarios continue normally, while the UI clearly identifies the BFF outage without losing valid unsaved input.
- **SC-024**: Operational verification can independently identify whether an injected failure originates in the UI, BFF, or core backend in 100% of tested service-outage scenarios.
- **SC-025**: Deployment evidence shows all three application services running as independent AKS workloads, with images sourced from ACR and no public route to the core service.
- **SC-026**: A failed rollout is detected by health, rollout, and compatibility checks and restores every service changed by that delivery attempt to its pre-attempt digest in reverse deployment order without rebuilding images; services not changed by the attempt retain their image digest and deployment state.
- **SC-027**: Authentication verification proves that no reusable employee token reaches the UI, every personalized core request carries a valid delegated core token, and each workload identity can access only its documented Azure dependencies.
- **SC-028**: Machine-consumer authorization tests accept only approved client identities with the required core API application role, scope roadmap/progress records to the validated application owner, and reject delegated tokens, wrong audiences, missing roles, unapproved clients, employee-owned references, and cross-application references.
- **SC-029**: Session-expiry tests revoke access at the 30-minute idle and 8-hour absolute boundaries across all BFF replicas, require reauthentication, and leave previously saved roadmaps and learning progress unchanged.
- **SC-030**: Retention verification demonstrates that departed-employee records are unavailable for normal personalized access immediately after departure is recognized; the `EmployeeIdentity` row and every profile/domain/idempotency/outbox owner link are deleted no later than day 90; and any retained security event is provably non-linkable, contains no tenant/object/owner identifier or reversible hash, and is irreversibly anonymized only after that deletion succeeds.
- **SC-031**: Entra lifecycle tests detect disabled or deleted accounts at sign-in and within 24 hours without a login, durably enqueue revocation before advancing reconciliation state, retry through a simulated BFF outage, obtain idempotent BFF acknowledgement and revoke every active session within the same 24-hour bound, and prevent new personalized access.
- **SC-032**: Concurrency tests demonstrate that identical retries and simultaneous submissions using one idempotency key create exactly one domain transition and return one established result, while mismatched payload reuse creates no additional state.
- **SC-033**: In delivery verification, a change affecting only one service validates and publishes only that service image, promotes the tested immutable digest, preserves the other two deployed digests, and stops before promotion when any required validation fails.
- **SC-034**: Every archived change plan contains mandatory UI/BFF/core and `contractIntegrity`/`readinessScenarios`/`performanceProfile`/`infrastructure` booleans. Shared contract, build-tooling, deployment, readiness-contract, traceability, readiness-manifest, performance-profile, and provision/teardown-skill changes select the exact services and validation lanes defined by the Jenkins delivery contract; documentation-only changes publish no application image while retaining contract-integrity validation. Multiple changed paths boolean-OR their selections without clearing an earlier `true` and retain sorted unique matching reasons. A genuine first or missing baseline is encoded only as JSON `null`, records `missing-baseline`, and conservatively selects every service and lane; a malformed or unresolvable supplied revision is rejected.
- **SC-035**: Concurrent or stale Jenkins builds cannot replace a newer environment state. An accepted delivery attempt that is aborted or loses a required Jenkins or Azure dependency before its first environment mutation leaves every digest unchanged; after a mutation, it restores every service changed by that attempt to its pre-attempt digest in reverse deployment order and verifies the restored environment before terminating.
- **SC-036**: A protected-branch delivery attempt begins only when Jenkins accepts the trigger and assigns a build identifier. Before requesting an ACI agent, Jenkins creates the authoritative controller-lifecycle audit record, which is explicitly non-authoritative as delivery evidence, containing the build identifier, source revision, start time, `result=pending`, and no failed stage; it finalizes that record exactly once with completion time, terminal result, and failed stage or `none`. Triggers not accepted because the controller is unavailable are not delivery attempts and remain observable in the source-control or webhook-delivery audit. Once an authenticated publisher or deployer ACI agent starts, Jenkins publishes all available non-secret change plans, release manifests, test results, digest resolutions, scan and SBOM summaries, deployment snapshots, and rollback evidence to the authoritative Azure Storage evidence container. Failure to provision an ACI agent or publish required evidence is finalized in the controller audit record, MUST NOT trigger a fallback Azure credential, and MUST block the next environment mutation or trigger attempt-wide rollback when a prior mutation has occurred. Controller restart/disk-loss verification MUST restore accepted lifecycle state from the replicated audit store with zero transition loss and within four hours, reconcile every accepted orphan exactly once, mark a recovered nonterminal orphan `aborted_recovered`, and prevent any new protected delivery until every post-mutation orphan has a verified rollback or approved terminal recovery result. A digest published before such failure but never promoted MUST remain `published_unpromoted` and ineligible for another build unless a new release manifest reruns every current gate.
- **SC-037**: Content review shows that 100% of published sessions either contain a qualifying lab or record an allowed omission reason.
- **SC-038**: Every active lab destination resolves only through domains present in the provider-allowlist version recorded at publication time.
- **SC-039**: Delivery verification proves that publisher stages cannot obtain the deployer identity, deployer stages cannot obtain the publisher identity, neither privileged template can start with an undeclared managed identity, and pull-request or unprotected-ref validation runs only on `azure-aci-validator`, which has no Azure managed identity and cannot obtain an Azure token or execute publication, promotion, deployment, verification, or rollback stages.
- **SC-040**: Rotation verification demonstrates uninterrupted existing sessions, new sign-in, and delegated core-token acquisition across all BFF replicas during normal rotation, removal of the retired credential within 48 hours, immediate rejection of an emergency-revoked credential, and a safe reauthentication path that preserves saved learning records when emergency revocation invalidates affected sessions.
- **SC-041**: Credential-lifecycle verification detects a provisioning credential with fewer than 30 valid days, proves that only the localhost least-privilege credential-manager role can replace the stable Jenkins credential entry without exposing secret material, successfully creates the identityless validator plus publisher and deployer ACI templates, and confirms that the retired credential can no longer provision container groups.
- **SC-042**: Evidence-retention verification demonstrates authorized reader access, denial for unapproved identities, hold-request creation/extension/release authority only for the Evidence Hold Managers group, direct Azure version-hold denial for that group, application of exact authorized version-level holds only by the isolated reconciler identity, rejection of reader/writer self-elevation, failure of every attempted replacement or deletion of an accepted evidence version, complete target-version enumeration, partial-set/clear failure reconciliation without false active/released state, automatic deletion after the fixed 90-day `immutable_until` when no legal hold exists, early-release inability to shorten that lock, preservation while a hold remains, deletion after release or expiry only once the base lock has elapsed, enforcement of the 180-day hold ceiling, and absence of prohibited credential and personal-data material.
- **SC-043**: Core performance verification MUST run two independent scenarios: roadmap creation and skill guidance. Each scenario MUST use 10 concurrent workers and the approved `tests/performance/performance-profile-v1.json`, whose full-profile and fixture-set digests cover its evidence schema and whose pinned BFF/core contract digests bind the exact operation pairing, beginner, intermediate, and advanced employee profiles, supported guidance topics, deterministic worker assignment, 45-second roadmap timeout, and 20-second guidance timeout. Before warm-up, CI MUST digest-match both contracts, regenerate the BFF-to-core mapper, reject committed or runtime mapper drift, and retain the exact mapper digest used. Each worker MUST complete two successful warm-up requests that are excluded from results, followed by 10 measured requests, producing 100 measured attempts per scenario. Core execution MUST be measured with a monotonic clock from core ASGI request entry through completion of response serialization, excluding BFF mapping/transport and browser rendering. Using the nearest-rank percentile calculation over all 100 measured values with failures/timeouts censored to the profile's scenario timeout, at least 95 of 100 valid roadmap attempts MUST complete within 30 seconds and at least 95 of 100 valid guidance attempts MUST complete within 10 seconds. Failed or timed-out valid requests MUST remain in the denominator and count as threshold failures. Evidence MUST record the performance-profile version, full-profile digest, fixture-set digest, BFF contract digest, core contract digest, generated mapper digest, environment, image digest, individual timings, raw outcome, percentile value, and calculated p95.
- **SC-044**: Bootstrap verification proves Terraform uses locked AzureRM/AzureAD providers and Azure Storage blob-lease-locked remote state; the external operator/consent approver receive only the exact time-bounded permissions/scopes in FR-071 and fail negative `Owner`, `Global Administrator`, standing/excess-scope, unrelated-app/data, and Jenkins-principal tests; bootstrap/data-principal commands cannot emit the environment manifest; the sole finalizer refuses emission until final Terraform state/configuration, data principals/denials, ALB Controller, and migration-policy attestations all pass; provider/quota attestations expire after seven days; and protected delivery is blocked for a missing, expired, stale, or inconsistent nonsecret manifest without granting Terraform apply/import/state-read or subscription-wide Reader permission to any Jenkins identity.
- **SC-045**: Migration verification proves dedicated-namespace Job/Pod/`pods/log` RBAC and negative exec/attach/port-forward/secret/service-account rights; non-overridable admission rejects a wrong image repository/digest form, command, target, service account, privilege, environment, volume, or deadline; publisher/deployer separation holds; the Job alone obtains the migration identity and DDL/backfill role; scoped PostgreSQL roles are mutually denied; and target/head/status/log/policy/timeout/retry success-or-failure evidence plus cleanup complete before core rollout.
- **SC-046**: Routing verification proves the legacy NGINX add-on/Ingress and public core route are absent; the pinned ALB Controller is installed before and owns the public UI/BFF Gateway; the browser output is the authoritative AGC HTTPS URL; and approved machines can reach core only from declared private networks through private DNS and a no-public-IP internal L4 LoadBalancer, with core-served rotated TLS, HTTPS probes, source controls, and valid app-role tokens.
- **SC-047**: Data-plane identity tests prove BFF-only Managed Redis access, core-only application DML, lifecycle-only known-identity status/timestamp plus reconciliation/checkpoint/outbox and unclaimed-retention-scheduling access with queue read/delete/claim/complete denial, retention-only security-definer claim/process-due procedure execution without direct queue or learning-row reads, lab-validation-only destination/status/counter access, migration-only DDL/backfill access, deployer database denial, and denial of passwords/access-key fallback and every documented cross-role attempt.
- **SC-048**: Controller-audit verification creates `pending` state before any ACI request with no workspace/executor, fsyncs it to both required controller-audit copies, does so when a repository Jenkinsfile omits audit calls or attempts to shadow audit code, survives agent provisioning failure and controller restart, restores after simulated controller-disk loss with zero accepted-transition loss and within four hours, reconciles accepted orphans against queue/run/webhook/Azure evidence, blocks protected scheduling until every post-mutation orphan is safe, synchronizes to authoritative evidence after authentication, and finalizes each accepted attempt exactly once through the pinned controller-installed `RunListener`/`RunAction` plugin.
- **SC-049**: Operational alert tests trigger public-gateway/private-core certificate warnings at 30/14/7 days and a critical page below 48 hours, directory-reconciliation warning/page at 6/8 hours without success, and active-lab warning/page at 30/36 hours plus immediate three-failure/unavailable alert, with every notification delivered to the exact Platform, Application, or Learning Content Operations route required by FR-061, FR-063, and FR-073.
- **SC-050**: Application interaction performance verification MUST run the eight independent scenarios approved in `tests/performance/interaction-performance-profile-v1.json`: focused-learning read, focused-learning mutation, review submission, progress persistence, BFF session bootstrap, BFF session refresh, BFF logout, and authentication callback. Each scenario MUST use 10 concurrent workers, two successful warm-up requests per worker, and 10 measured requests per worker for exactly 100 measured attempts. At least 95 of 100 attempts per scenario MUST complete within 5 seconds. Failures and timeouts MUST remain in the denominator and contribute the 5-second timeout value to nearest-rank p95. The approved profile and `tests/performance/interaction-performance-fixtures-v1.json` MUST fix the exact operation/route, schema-valid state and request values, UUID/token/timestamp derivation, deterministic fixture assignment, measurement start/stop boundary, hard timeout, BFF/core contract digests, fixture-set digest, full-profile digest, evidence schema, and required raw and aggregate evidence. The authentication-callback scenario is a deterministic post-exchange application-processing benchmark: employee interaction, live Entra authorization/code exchange, and external provider network time are excluded; timing begins at the first BFF callback-handler instruction after the approved in-process provider fixture returns a validated token result and ends when the BFF emits the post-bootstrap redirect.

For every success criterion using “100%,” “all tested,” or “exactly one,” the committed cases, denominator, applicability, and pass calculation MUST follow `contracts/implementation-readiness-contract.md` §Universal scenario denominator policy and the digest-verified `tests/fixtures/readiness-scenario-manifest-v1.yaml`. Setup failure, unavailable dependency, crash, timeout, or missing evidence MUST count as a failed case rather than an exclusion; only a not-applicable outcome explicitly declared by the primary-journey matrix before execution may be omitted, and it MUST NOT reduce another criterion's stated denominator.

## Assumptions

- `contracts/implementation-readiness-contract.md` is a normative refinement of these assumptions and requirements. Its primary-journey/dependency matrices, topic/lab lifecycle, learning timing/versioning, persistence/idempotency/capability rules, pilot and measurement protocols, universal scenario denominators, rotation/accessibility/browser matrices, non-production service-level decisions, and prerequisite register MUST be used for implementation and verification; a conflict with an authoritative OpenAPI, security, or delivery contract blocks release.
- In this specification, the "first release" means the complete four-journey internal pilot release. The US1 roadmap checkpoint is a non-release MVP demonstration and does not claim the release-wide navigation, deployment, browser-matrix, or pilot outcomes. The first release uses Microsoft Entra ID organizational sign-in and does not add application-managed registration, passwords, administrative roles, or account management.
- The existing roadmap, skill-guidance, progress-check-in, and health capabilities remain the authoritative source of application behavior.
- Existing machine consumers receive dedicated Entra application identities and
  core API app-role assignments; their stateful roadmap/progress records are
  application-owned, and anonymous compatibility is not preserved.
- Aggregate telemetry may outlive personalized learning records only when it
  cannot be linked back to the departed employee.
- Daily Entra lifecycle reconciliation is an authorized background
  machine-to-machine operation and requests only the directory permissions
  required to determine whether known employee identities remain active.
- English is the only supported language in the first release.
- The UI, BFF, and core backend are three separate deployable services with independent runtime configuration, health monitoring, release, rollback, and scaling lifecycles.
- All three services run on the existing non-production Azure AKS platform; Azure-managed backing services are preferred over in-cluster stateful components.
- The UI service owns presentation, the BFF owns UI-oriented session mediation and orchestration, and the core backend owns authoritative authorization, business rules, and persistent data.
- The UI communicates with the BFF; it does not directly call personalized core-backend interfaces.
- The backend remains the authoritative owner of business rules and persistent learning data; the UI does not connect directly to backend storage.
- Each deployed environment provides approved UI-to-BFF and BFF-to-backend destinations and trust configuration outside source code.
- Active-session convenience state may be temporary; cross-device history, long-term drafts, and user-specific saved dashboards are outside this feature.
- At implementation verification time, the first release supports the latest two stable major releases of Google Chrome, Microsoft Edge, Mozilla Firefox, and Safari/WebKit. The existing 320, 375, 768, 1024, 1440, and 1920 pixel viewport matrix applies across the supported desktop engines, with Chrome and Safari/WebKit mobile-device emulation covering touch and mobile-width behavior; exact OS, orientation, zoom/text-scaling, keyboard, NVDA, and VoiceOver coverage follows `contracts/implementation-readiness-contract.md` §Accessibility behavior and browser boundary.
- Pilot usability evaluation is reduced to one eligible internal employee for this technical PoC. It is exploratory and must not be represented as a statistically representative release pilot. The participant completes the desktop and mobile-width scenarios in the frozen protocol, task completion is recorded independently from satisfaction ratings, and anyone who contributed to implementation is excluded. Eligibility, task script, assistance classification, exact questionnaire wording, event collection, and 1/1 pass calculations follow `contracts/implementation-readiness-contract.md` §Pilot usability protocol.
- Visual branding uses a clear, professional default presentation; a formal design system or custom brand package is not required for the first release.
- The first release implements and verifies the explicit accessibility behaviors in FR-013, FR-014, SC-005, SC-006, and `contracts/implementation-readiness-contract.md` §Accessibility behavior and browser boundary. Formal certification of complete WCAG 2.2 Level AA conformance is outside this feature.
- Advanced analytics, learner-facing notifications, content-authoring tooling, and administrative reporting are outside this feature. Operational delivery, credential, certificate, lifecycle, dependency, and lab alerts/notifications explicitly required by FR-061, FR-063, FR-064, FR-069, FR-073, and their normative contracts are not excluded.
- Hands-on environments are operated by external providers; creating or managing temporary lab infrastructure is outside the first release.
- The initial lab-provider allowlist is empty. A provider becomes eligible only after approval by members of the configured Learning Content Owners and Application Security Reviewers Entra groups. Group object IDs are supplied as environment configuration and are not embedded in source code.
- The Jenkins controller runs locally at `http://localhost:8080` and its
  existing Jenkins Azure cloud node named `azure` creates ephemeral
  identityless validator plus publisher and deployer agents in the configured
  resource group. The validator has no Azure managed identity; each privileged
  template uses a distinct user-assigned managed identity for its ACR or AKS scope. The
  existing Jenkins Azure cloud-node configuration provisions and connects the
  agents; no new VPN, public controller endpoint, or inbound agent connection is
  introduced by this feature. The existing cloud-provisioning service principal
  is the only controller-held Azure credential and is excluded from ACR push and
  AKS deployment access; the controller stores no kubeconfig or additional
  Azure delivery credential.
- Competitive leaderboards, peer rankings, points, and streak-based rewards are outside the first release.
