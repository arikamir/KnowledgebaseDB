interface:
  display_name: "Teardown Azure App Resources"
  short_description: "Safely destroy the complete app and identity topology"
  default_prompt: "Use $teardown-azure-app-resources to verify state parity, plan, and safely remove the complete feature-003 Azure topology without orphaning identities or roles."
