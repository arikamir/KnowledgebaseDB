interface:
  display_name: "Provision Azure App Resources"
  short_description: "Auto-create missing Azure deployment infrastructure"
  default_prompt: "Use $provision-azure-app-resources to ensure required Azure infrastructure exists, then continue the requested application deployment."
