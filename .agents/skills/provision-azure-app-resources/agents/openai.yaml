interface:
  display_name: "Provision Azure App Resources"
  short_description: "Create the complete feature-003 Azure and identity topology"
  default_prompt: "Use $provision-azure-app-resources to validate or create the complete three-service AGC/AKS/data/evidence topology and all least-privilege identities, then continue the requested deployment."
