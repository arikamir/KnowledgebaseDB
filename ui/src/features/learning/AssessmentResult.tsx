import { achievementTier } from "../../app/learner-state-mappers";
export function AssessmentResult({ scorePercent, evidence = [] }: { scorePercent: number; evidence?: string[] }) { const tier = achievementTier(scorePercent); return <section className="assessment-result" aria-labelledby="assessment-result-title"><h2 id="assessment-result-title">Assessment result</h2><p><strong>{scorePercent}%</strong> · {tier ? `${tier} achievement` : "Not passed yet"}</p>{evidence.length > 0 && <ul aria-label="Assessment evidence">{evidence.map((item) => <li key={item}>{item}</li>)}</ul>}</section>; }
